---
title: "Authorization Header란?"
date: "2025-12-21"
description: "HTTP 요청을 보낼 때 서버에게 인증 정보를 전달하는 Authorization 헤더의 개념과 Basic, Bearer 인증 타입을 알아봅니다."
categories: ["Auth"]
tags: ["http", "header", "authorization", "authentication"]
series: "HTTP 인증 시리즈"
seriesOrder: 1
---

# Authorization Header란?

HTTP 요청을 보낼 때 "나는 누구다"라는 것을 서버에게 알려주는 방법입니다.
HTTP 헤더 중 하나로, 클라이언트가 자신의 신원을 증명하기 위해 사용합니다.

```
Authorization: <type> <credentials>
```

## 인증 타입 종류

| 타입 | 설명 | 예시 |
|------|------|------|
| Basic | 사용자ID:비밀번호를 Base64 인코딩 | `Basic dXNlcjpwYXNz` |
| Bearer | OAuth 2.0 토큰 | `Bearer eyJhbGci...` |
| Digest | 해시 기반 인증 | `Digest username="user"...` |

## Basic 인증

가장 간단한 인증 방식입니다. `사용자ID:비밀번호`를 Base64로 인코딩하여 전송합니다.

```
Authorization: Basic base64(username:password)
```

**예시:**
```http
GET /api/users HTTP/1.1
Host: example.com
Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM=
```

> Base64는 암호화가 아니므로, **반드시 HTTPS와 함께 사용**해야 합니다.

## Bearer 인증

OAuth 2.0에서 사용하는 토큰 기반 인증 방식입니다. "Bearer"는 "소유자"라는 뜻으로, 토큰을 가진 사람에게 권한을 부여합니다.

```
Authorization: Bearer <token>
```

**예시:**
```http
GET /api/users HTTP/1.1
Host: example.com
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 응답 코드

| 상황 | 응답 코드 |
|------|----------|
| 인증 성공 | `200 OK` |
| 인증 헤더 누락 | `401 Unauthorized` |
| 인증 정보 있지만 권한 없음 | `403 Forbidden` |

## 참고

- [RFC 7235 - HTTP Authentication](https://datatracker.ietf.org/doc/html/rfc7235)
- [RFC 6750 - Bearer Token](https://datatracker.ietf.org/doc/html/rfc6750)
- [RFC 7617 - Basic Authentication](https://datatracker.ietf.org/doc/html/rfc7617)
